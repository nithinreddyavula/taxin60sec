"use client";
import AppShell from "@/components/AppShell";
import { useAppSession } from "@/components/AppProviders";
export default function ProfilePage() { const { user } = useAppSession(); return <AppShell><p className="eyebrow">Account</p><h1 className="mt-2 text-3xl font-bold">Profile & settings</h1><section className="card-dark mt-8 max-w-2xl p-6"><h2 className="font-bold">Your account</h2><dl className="mt-5 grid gap-5 sm:grid-cols-2"><div><dt className="text-sm text-secondary">Name</dt><dd className="mt-1 font-semibold">{user?.name ?? "Not provided"}</dd></div><div><dt className="text-sm text-secondary">Email</dt><dd className="mt-1 font-semibold">{user?.email ?? "Not provided"}</dd></div><div><dt className="text-sm text-secondary">Access</dt><dd className="mt-1 font-semibold">{user?.roles.map((role) => (typeof role === "string" ? role : (role as { name: string }).name).replace("ROLE_", "")).join(", ")}</dd></div></dl></section></AppShell>; }