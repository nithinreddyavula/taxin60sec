import type { Metadata } from "next";
export const metadata: Metadata = { title: "Apply as a CA Partner", description: "Apply to become a TaxIn60Sec Chartered Accountant partner.", alternates: { canonical: "/ca-apply" } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
